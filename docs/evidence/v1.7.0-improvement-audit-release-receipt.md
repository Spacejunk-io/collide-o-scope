# v1.7.0 improvement-audit pre-release receipt

Date prepared: 2026-08-24<br>
Status: **pre-release evidence ledger; not a final release receipt**<br>
Proposed version: **v1.7.0**<br>
Final candidate commit: **PENDING**<br>
Exact-commit CI/adversarial result: **PENDING**
Tag, signatures, attestation, publication, and redownload verification: **NOT RUN**

This receipt consolidates the evidence produced while enacting the post-v1.6.0
improvement audit. It records retained work, deliberately stopped prototypes,
and gates that remain external or unexecuted. An implementation or focused test
recorded by a tranche receipt is not silently upgraded here into proof that the
final release candidate passed that test. Final-candidate claims may be added
only after the exact candidate SHA exists and the named gate has actually run.

## Provenance boundary

| Fact | Value |
| --- | --- |
| Audited release | `v1.6.0` |
| Audited commit | `000411d383205596b46feb09de32f7eaa5aeb0c6` |
| Audit | `COLLIDE_O_SCOPE_IMPROVEMENT_AUDIT.md` |
| Audit SHA-256 | `8AA8B84BD86E91AE4B88A2D0955494B1E88D0CFA66AFB971E7181ED36285CD5B` |
| Audit date | 2026-08-23 |
| Frozen baseline receipt | [`post-v1.6.0-improvement-baseline-receipt.json`](post-v1.6.0-improvement-baseline-receipt.json) |
| Working-tree parent observed while drafting | `5d348aa1e2f13563cb372d28a61e4bea4e30015b` (`docs: freeze post-v1.6 improvement baseline`) |
| Final candidate | Not yet created; the working-tree parent is not a release SHA |

The audit file is intentionally ignored by the repository's existing root
`/*.md` rule. Its digest above identifies the exact blueprint without claiming
that the audit itself is a release artifact. The baseline receipt records a
clean tracked v1.6.0 measurement boundary, Rust 1.98.0, FFmpeg 8.1.2, the host
identity, and 1,753 passing non-ignored tests. It explicitly made no pre-P1 GPU
or photon-latency claim.

## Reading this ledger

- **Implemented** means the cited evidence supports the named bounded contract
  or production surface. It does not imply every physical or performance gate.
- **Retained** means independently safe implementation or evidence remains,
  while a broader promotion gate is still open.
- **Evaluation/research** means the work is not an advertised production
  capability.
- **Stopped/deferred** means the audit's stop law fired or required evidence is
  absent. It is an intentional truthful result, not a release waiver.
- Test counts and measurements below belong to the cited intermediate receipt
  and host unless an exact final-candidate result is added later.

## P0-P10 disposition

### P0 — transactional control listeners and TLS identity

**Disposition: implemented contract; physical network evaluation remains.**

The executable capability registry records independent IPv4/IPv6 loopback HTTP
listeners, fail-closed LAN HTTPS, separately reported listener state, and a
versioned/synced/atomically replaced TLS identity. Browser, native, and backend
surfaces are represented as implemented. The authoritative limitation remains
second-host reachability plus packet-capture proof that no token, cookie,
action, thumbnail, or upload crosses the LAN in plaintext. No such physical
network receipt is claimed here. Authority:
[`p10-capability-campaign-truth-closure.md`](p10-capability-campaign-truth-closure.md).

### P1 — correlated engine/GPU timing and action-to-photon separation

**Disposition: retained measurement spine; physical and overhead gates open.**

Bounded ingress-to-apply/apply-to-submit correlation and asynchronous six-stage
GPU timing are surfaced through native/browser Stage Health, with timestamp
support remaining adapter-conditional. Engine submission is never labeled
photon time. The optical analyzer and refusal law exist, but the required
electrically coupled LED, optical sensor, and controlled display were not
available; physical p50/p95/p99 are therefore null and P6 promotion is
forbidden. The fixed-fixture instrumentation overhead gate (`<1%` or `0.20 ms`,
as applicable) is also unexecuted.

The retained support-bundle recorder uses a fixed nonblocking producer queue,
30–60 second validated rotation policy, fixed total byte cap, marker-validated
completed files, typed path-free stage/worker/action/error facts, hostile
privacy fixtures, and a real killed-subprocess restart proof. Its focused suite
passed 9 tests with the subprocess helper intentionally ignored and invoked by
the passing parent; the production decoder-worker aggregation regression also
passed. Authority:
[`p1-action-to-photon-fixture-protocol.md`](p1-action-to-photon-fixture-protocol.md),
[`p1-action-to-photon-fixture-unexecuted.json`](p1-action-to-photon-fixture-unexecuted.json),
[`p1-flight-recorder-closure.md`](p1-flight-recorder-closure.md),
and the P10 closure receipt.

### P2 — structural reuse and persistent core GPU objects

**Disposition: P2a address/offset safe subset and P2b retained; broader P2
performance promotion remains open.**

P2a retains the audit-authorized safe subset: one stable-ID topology signature,
cached `StableModAddressBook`, retained offset storage, exact live/export
semantics, named invalidation counters, and forced-recompile repair. Five
focused tests passed. It explicitly stops before Study/render topology and
does not claim the broader structural plan or 8/24-layer planning p99.

P2b retains aligned renderer-owned uniform arenas, bounded source/view caches,
generation-complete invalidation, lazy optional allocation, physical-byte
ledger reconciliation, and warmed zero-construction fixtures across named core
paths. The receipt records a 1,853-test non-ignored binary run and focused
physical-GPU compatibility fixtures on its intermediate tree. It expressly
does not turn zero construction counts into a latency claim. A representative
warmed command-encoding p95/p99 result must still show at least 10% improvement
or a result below 0.5 ms, and the one/two-layer non-regression gate remains.

The broader P2a structural-compilation and 8/24-layer planning-p99 gate remains
open and must not be inferred from the retained address-only subset. Authority:
[`p2a-stable-mod-topology-cache.md`](p2a-stable-mod-topology-cache.md) and
[`p2b-core-gpu-object-retention.md`](p2b-core-gpu-object-retention.md).

### P3 — serial-path durability, publication, retirement, and indexing

**Disposition: implemented source-level subtranches; only P3b has a dedicated
closure receipt.**

- P3a has one in-flight recovery write plus one newest-wins pending request,
  and acknowledges only a verified durable sequence with a bounded shutdown
  flush.
- P3b has wire-v2 authored/live revision separation, 12 Hz live publication,
  zero-receiver silence, one newest retained state generation, separate
  bounded action dispositions, and event/fallback-driven monitor inventory.
- P3c has generation-qualified decoder retirement through a bounded supervisor
  and deadline drain rather than render-thread joining.
- P3d has the shared same-directory staged/synced publication law for patches
  and bounded no-replace upload admission, deadlines, cancellation, and orphan
  cleanup.
- P3e has generation-checked bounded asynchronous library scanning and page
  snapshots.

P3b's automated contract is green, while physical hot-plug/unplug and a
ten-minute multi-monitor churn fixture were not run. P3a/P3c/P3d/P3e presently
lack dedicated consolidated receipts; their source-level implementations and
focused tests must not be represented as completion of the audit's slow-disk
p99, rapid-retirement/RSS/handle, every-stage crash, or 100,000-entry
local/slow-network keep gates. Authority:
[`p3b-revision-split-browser-publication.md`](p3b-revision-split-browser-publication.md)
and [`p3b-browser-publication-receipt.json`](p3b-browser-publication-receipt.json).

### P4 — immutable payloads, source truth, and planar delivery

**Disposition: P4a retained; P4b truth retained with renderer stop; P4c
stopped as a software prototype.**

- P4a freezes decoded RGBA payloads behind one physical allocation, shares
  identity through cache/upload/read-only owners, reuses bounded pools, and
  drains upload validation asynchronously. Its focused suites and full
  non-ignored binary run passed on the receipt tree. The prescribed two-source
  decoded-materialization/upload p99 comparison was unavailable, so no latency
  improvement is claimed.
- P4b carries bounded provenance-aware color and display descriptors through
  decoder telemetry, Stage Health, proxy receipts, and export provenance.
  Declared matrix/range conversion and legacy unspecified fallback are tested.
  Clean aperture, SAR, rotation, and mirror are **not applied by the renderer**;
  the source-UV law remains a reference until an immutable evaluated-plan seam,
  legacy identity proof, and GPU measurement exist.
- P4c retains a bounded immutable YUV420P/NV12/P010 contract and CPU oracle,
  but no decoder selects it, no planar GPU textures or conversion pass exist,
  and no patch surface changed. The required common-8-bit byte reduction,
  CPU/GPU equality, and decode/upload/total-frame percentiles were not measured.

Authority: [`p4a-immutable-payload-upload-validation.md`](p4a-immutable-payload-upload-validation.md),
[`p4b-source-descriptor-stop-receipt.md`](p4b-source-descriptor-stop-receipt.md),
and [`p4c-planar-delivery-stop-receipt.md`](p4c-planar-delivery-stop-receipt.md).

### P5 — copy-free Legacy Exact ping-pong

**Disposition: ordinary copy removal retained; pass fusion rejected.**

The ordinary Legacy Exact accumulator now alternates retained identities and
lands on the established slot-0 publication seam without a full-frame
normalization copy. The receipt accounts for eliminated copies (up to
74,649,600 bytes/frame in its eight-layer 1080p example), retains semantic
time/ownership copies, and records fixed-golden/live-export and downstream GPU
fixtures on the audit host. No fusion ships: the sRGB store/sample rounding
boundary lacks cross-adapter exactness proof and the required affected-frame
p95/p99 improvement receipt does not exist. The explicitly required
`videos/audit.mp4` Mosh/VHS/performance-recorder fixture was absent. Authority:
[`p5-copy-free-ping-pong.md`](p5-copy-free-ping-pong.md).

### P6 — Stable and Low-Latency presentation profiles

**Disposition: Stable retained; Low Latency stopped before promotion.**

The pure profile/scheduler contract keeps Stable at the exact 30 Hz/FIFO/
latency-2 baseline and describes a host-specific 60 Hz candidate without
changing offline cadence. No qualifying optical campaign, PresentMon campaign,
ten-minute 60 Hz present run, or exact host calibration exists. There is no
Low-Latency control or availability claim. Authority:
[`p6-presentation-profile-candidate.md`](p6-presentation-profile-candidate.md).

### P7 — GPU-loss recovery

**Disposition: supervised Phase A retained; in-process Phase B stopped.**

Device loss maps to exit code 75 after bounded writer/decoder shutdown, and the
supervisor may relaunch once into the explicit operator recovery surface.
Generation-qualified stale completions and the recovery state machine have
deterministic tests. The injection seams are not real driver resets; packaged
relaunch-to-surface timing has not run, transparent Program continuity is not
claimed, and in-process resource rebuilding remains disabled. Authority:
[`p7-gpu-loss-phase-a.md`](p7-gpu-loss-phase-a.md).

### P8 — adversarial and performance verification

**Disposition: retained partial closure; full promotion gate open.**

Nine bounded in-memory fuzz targets, fixed-seed 10,000-case history property
testing, a Loom publication model with a caught broken variant, deterministic
fault seams, a fixed-storage synthetic soak, and six Criterion smoke groups are
present. The recorded 100,000-step synthetic run drained its bounded
publication state and produced its named digest. These are not a substitute
for the missing physical/runtime campaigns.

Local Windows fuzz execution did not run because the pinned nightly's LLVM 23
ASan runtime was unavailable against the installed LLVM 22 runtime. The
tag/push workflow is configured, but no final-candidate workflow result is
claimed. Live worker mailboxes have not all migrated to the modeled kernel;
three named injectors, container-metadata fuzzing, approved repeated p95/p99
baselines, actual GPU command encoding, heterogeneous hardware, and the
one-hour mixed-media RSS/GPU-ledger/deadlock/artifact soak remain open.
Authority: [`p8-adversarial-verification-closure.md`](p8-adversarial-verification-closure.md).

### P9 — reproducible, attributable, dependency-audited release chain

**Disposition: local policy/preflight implemented; release execution pending.**

The current release-trust implementation pins Rust and every workflow action,
gates publication on the newest CI and adversarial runs for the exact commit,
binds each required platform/dependency job and complete step body to its
reviewed fail-fast command, and rejects failed or misattributed log summaries.
It checks cargo-deny/cargo-audit plus owned and expiring exceptions, verifies the
vendored wgpu-hal archive/upstream-plus-patch relation with seeded drift
rejection, creates a path-free dependency-license inventory, and prepares two
clean unsigned Windows builds for byte comparison. It also prepares a
CycloneDX SBOM, deterministic archives, checksums, BuildIdentity-bound
provenance, a checked technical native-license review, Sigstore keyless
checksum signing, signer-workflow/source-ref/source-digest-bound GitHub
attestation of every initial asset, create-only draft assembly, publish-last,
and post-publication redownload verification.
One commit is resolved by the verification gate and passed immutably to both
independent builds and the redownload verifier; annotated tags are peeled and
rechecked against origin immediately before publication and after redownload.
The verifier requires exact top-level checksum, directory, and provenance
artifact inventories, authenticates `SHA256SUMS` before executing downloaded
code, rejects portable/Windows ZIP traversal and aliasing, and confines every
extracted member to a fresh verification root.

After freshly redownloading and verifying the ten immutable initial assets from
the exact ID-bound draft, the workflow prepares a separate closed-schema
final-release receipt.
It cross-links the exact tag/SHA and selected workflow runs, final validation
counts (including ignored/external fixtures), reproducible-build and
BuildIdentity facts, dependency/exception/vendor evidence, every asset hash and
attestation, Sigstore identity, explicit unavailable/unsigned Authenticode
disposition, release URL, and downloaded-package report. The receipt freezes
before its own keyless signature and attestation, avoiding a circular claim.
The workflow uploads that receipt and its bundle without overwrite, updates the
draft body from the same bounded summary, then freshly redownloads and verifies
all 12 draft assets, their exact names/hashes, authenticated checksum/package
associations, Sigstore material, and attestations. After one immediate remote
annotated-tag recheck, the sole public mutation flips the complete draft to a
release. A new directory then receives all 12 published assets for ordinal
name/byte comparison, the public URL/body/state are re-read case-sensitively,
and a final remote annotated-tag recheck ends the trust sequence; no mutation
follows it. A bounded authenticated release-list preflight rejects an existing
same-tag draft or published release, so a rerun cannot replace prior evidence.
Authority:
[`p9-final-release-governance-hardening.md`](p9-final-release-governance-hardening.md).

The package policy requires the project license and copyright notice plus the
pinned Gyan/FFmpeg GPL text, README/source identity, and runtime `-buildconf`.
The exact reviewed Windows input is FFmpeg 8.1.2, archive SHA-256
`274923c68904a9b76c73b908f57923dafba81155856cd742138515ded570d066`,
upstream source commit `38b88335f99e76ed89ff3c93f877fdefce736c13`.
The review and generated inventory are mechanical provenance, explicitly not
legal advice.

Local preflight passed the dependency exception checker, `cargo deny` advisory/
ban/source and license gates, cargo-audit, online/offline vendor self-tests,
path-normalization self-test and real inventory, exact-workflow-gate self-test,
release-verifier and final-receipt mutation self-tests, workflow pin checker,
YAML/Python/PowerShell/actionlint parsing, reviewed FFmpeg hash/
license/build-configuration comparison, unsigned-PE inspection, and
`git diff --check`. The BuildIdentity normalization and release-tool status
tests passed 7/7, including the real Gyan suffixed version form, clean
porcelain empty-output handling, and the reviewed MSVC linker-help exit status.
These results occurred before a final candidate commit and therefore are not
release completion evidence.

No managed Authenticode certificate or remote service exists. Reproducible PE
inputs and the package must remain unsigned, and the evidence states
`unsigned_unavailable`; Sigstore authenticates checksum/provenance material,
not the PE trust chain. This is a hard stop on any Authenticode claim. If project
policy later makes Authenticode mandatory, publication must stop until a real
managed service exists and signing occurs only after unsigned comparison.

The following P9 gates are **not run**: two clean builds from the final SHA,
final byte comparison, final SBOM/provenance, exact-SHA CI/adversarial success,
tag resolution, Sigstore signature, GitHub attestation, GitHub release,
redownloaded asset execution/identity comparison, and clean-host verification.

### P10 — executable capability and campaign truth

**Disposition: implementation and generated contradiction gates retained;
final-candidate rerun pending.**

The registry uses the closed runtime vocabulary `Implemented`,
`EvaluationRequired`, `Deferred`, `RejectedByMeasurement`, and
`UnavailableOnPlatform`, with typed surface facts, reasons, limitations, and
evidence IDs. The generated JSON/Markdown/README surfaces and campaign status
gate prevent P0/P1/P4b/P7 or D1-D5 from being advertised beyond their evidence.
The current closure receipts record a passing generator check, 10 passing
capability truth tests, 3 passing generator/contradiction tests, 6 TLS-identity
tests, 6 action-correlation tests, 4 GPU-timing tests, 7 source-descriptor tests
(with 1 external-FFmpeg fixture ignored), 4 supervised-recovery model tests,
strict Clippy, and focused Rustfmt. These are focused intermediate-tree results,
not a substitute for the final-candidate suite. Authority:
[`p10-capability-campaign-truth-closure.md`](p10-capability-campaign-truth-closure.md)
and
[`p10-external-deferred-capability-evidence-boundary.md`](p10-external-deferred-capability-evidence-boundary.md).

The same P10 receipt also records an exact passing physical-GPU remediation
transaction: ordinary preflight derives the typed Master-bypass diagnostic,
revision-bound candidate, and planner-evaluated immutable preview from a real
refused authored rack; cancel/preview are no-ops; normal confirmation records
history and recovery; the exact refused rack then passes detached preflight;
and ordinary undo/redo restore exact canonical states. The fixture contains no
manually injected diagnostic, candidate, operation, or preview. This is a
focused host result and does not close any separate optical or p95/p99 gate.

## R1-R2 creative research disposition

| ID | Retained result | Stop/defer boundary | Authority |
| --- | --- | --- | --- |
| R1 | Bounded Cut/Dissolve planner and CPU premultiplied reference; all-or-none Scene admission, deterministic tick/beat phase, two simultaneous dissolve cap, zero-resource legacy Cut | No live outgoing-source retention, shader, authoring, reaper integration, live/export pixel receipt, or three-layer 720p p95/p99 campaign. Dissolve remains research-only. | [`r1-prepared-source-transition-kernel.md`](r1-prepared-source-transition-kernel.md) |
| R2 | Data-only planner for at most two stable Mosh domains with distinct bounded history/mailbox/worker/resource identities and permanent tombstones | No codec worker, GPU path, patch/panel/Morph/modulation/Dice/export surface, two-domain 720p30 p95/p99, or named artistic recording review. Existing single final-program Mosh remains authoritative. | [`r2-two-mosh-domain-planner-research.md`](r2-two-mosh-domain-planner-research.md) |

Neither research prototype is an advertised v1.7.0 capability.

## D1-D5 campaign disposition

The campaign status file and P10 contradiction gate are authoritative for the
closed campaign vocabulary.

| ID | Campaign status | Truthful retained scope | Remaining gate/limit | Authority |
| --- | --- | --- | --- | --- |
| D1 | `implemented` / complete | Study motion ABI 1.1 appends six typed operations without renumbering ABI 1.0; CPU/GPU, planner, production-pixel, and available-adapter receipts exist | ABI 1.0 remains frozen; no claim of universal adapter coverage | [`d1-study-motion-abi-1.1.md`](../rfcs/d1-study-motion-abi-1.1.md) |
| D2 | `evaluation` | Bounded read-only CPU/GPU photosensitivity advisor prototype with aggregate-only telemetry and an available-adapter equality receipt | Not constructed by the application; accessibility/legal review and paired P1 overhead p50/p95/p99 are absent; it is not medical/regulatory assurance | [`d2-photosensitivity-advisor-prototype.md`](d2-photosensitivity-advisor-prototype.md) |
| D3 | `retained` | Deterministic inspect/build/import core with canonical identities, hostile-path limits, staged publication, and 12 passing focused tests | No operator UI or machine-A to clean-machine-B live/export reproduction | [`d3-portable-show-bundle.md`](d3-portable-show-bundle.md) |
| D4 | `implemented` / complete | One typed post-validation accepted-mutation boundary covers all seven source classes, preserves v1 performance bytes/hash, deduplicates canonically, and excludes replay/safety/refused work | Stops at frozen v1 controls; no topology/routes/Scenes/raw events/origin serialization/v2 format | [`d4-accepted-creative-mutation.md`](d4-accepted-creative-mutation.md) |
| D5 | `retained` | Named pre-opaque straight-alpha seam plus exact PNG, FFV1/gbrap, and fill/key transactional publishers; physical exact round-trip fixtures passed on its receipt host | No application action, live acquisition/encoder, invoked offline readback loop, or P1 multi-flight readback/performance receipt; no advertised alpha capability | [`d5-straight-alpha-export.md`](d5-straight-alpha-export.md) |

## Retained scope versus fired stop gates

The release may truthfully retain bounded foundations even where promotion
stopped: P1 engine/GPU measurement and bounded flight recorder, P2a stable
address/offset caching, P2b GPU-object retention, P3 ownership
primitives and P3b publication, P4a payload ownership, P4b descriptor truth,
P5 copy subtraction, P7 supervised recovery, P8 deterministic/adversarial
infrastructure, P9 release policy, P10 capability truth, D1, D4, and the bounded
cores of D2/D3/D5. Retaining code does not make a stopped surface available.

The following stops remain binding:

- P2 performance wins are not established by allocation counters.
- P4b display geometry is not applied; P4c has no production planar GPU path.
- P5 fusion is rejected; P6 Low Latency is unavailable.
- P7 Phase B and transparent continuity are unavailable.
- P8 is partial, not a completed fuzz/soak/performance campaign.
- P9 has not produced or published a final artifact, and has no Authenticode.
- R1/R2 are research-only; D2/D3/D5 have no advertised operator capability.
- Exact live VFR transport and live recorder audio mux remain deferred.
- D3D11VA/hardware decode remains evaluation-only; full-16 temporal history
  remains rejected by its existing resource/benefit measurement.
- Automatic proxy adoption, automatic quality/raster reduction, per-layer
  unbounded Mosh/VHS workers, global RAM/VRAM cap increases, and other audit-
  rejected shortcuts remain rejected.

## Common physical/performance matrix not completed

Individual receipts include meaningful host and physical-GPU tests, but no one
run has executed the audit's common matrix over the final candidate. The
following omissions must remain visible:

1. No electrically coupled input/optical-display action-to-photon campaign;
   no physical p50/p95/p99 and no P6 promotion basis.
2. No second-host/network-namespace LAN reachability plus packet-capture
   campaign for P0.
3. No complete one/three/eight-layer 720p/1080p matrix spanning Legacy Exact,
   Advanced, Temporal, Motion, Mosh, VHS, bypass, taps, recorder, StageMap,
   blackout, and idle/normal/saturated/reconnect controller traffic.
4. No common corrected two-source 720p30 H.264/Cygnus comparison; P4's paired
   decode-delivery/upload percentiles remain absent. The P5-required local
   `videos/audit.mp4` fixture was also absent.
5. No repeated five-run raw p50/p95/p99 campaign after a 300-tick warm-up for
   P1 overhead, P2 planning/encoding, P4 delivery/upload, or P5 fusion.
6. No ten-minute physical multi-monitor hot-plug/unplug/window-migration run,
   no ten-minute admitted 60 Hz presentation run, and no real packaged
   GPU-device-loss/relaunch deadline measurement.
7. No one-hour real mixed H.264/VP9/VFR, effects, proxy, recording, output-
   toggle soak measuring process RSS, GPU ledger/object growth, deadlock, and
   partial-artifact behavior. The P8 fixed-storage run is a synthetic stand-in.
8. No complete heterogeneous adapter/backend/driver/display/present-mode
   matrix; available-host GPU proofs do not generalize to every platform.
9. No physical phone gyro/touch/Wi-Fi, MIDI feedback, audio input, Spout pair,
   full-screen transition, or venue StageMap receipt.
10. No clean-machine D3 bundle reproduction, independent D2 accessibility/
    legal review, or D5 end-to-end live alpha acquisition/readback campaign.

The two large untracked WebM files are not substitutes for these named
fixtures, and this receipt does not infer a test from their presence.

## Protected untracked boundary

The following user-owned untracked files were outside every edit, package,
test-fixture, and evidence-generation action for this audit enactment:

- `.da-vinci-canon-pre-refinement-backup-20260822.zip`
- `4K_Nature_Cinematography_recorded_with_Nikon_D5300.webm.1080p.vp9.webm`
- `Black_swan_(Cygnus_atratus).webm.1080p.vp9.webm`

They remain untracked and untouched. They must not be added, removed, renamed,
hashed into release evidence, or included in a source/release archive.

## Why v1.7.0 is the appropriate version

The retained scope is materially larger than a patch release: it adds security
and lifecycle contracts, timing/diagnostic schemas, bounded ownership and
publication primitives, source-truth telemetry, verification/release
infrastructure, capability facts, and additive creative contracts. That earns
a minor version.

No intentional compatibility break justifies v2.0. Legacy/absent fields retain
their prior behavior; Stable remains the presentation default; packed RGBA,
single final-program Mosh, opaque MP4, ABI 1.0, performance-take v1, and legacy
pixel paths remain authoritative unless an operator selects an admitted
additive surface. Stopped research is not advertised. Therefore **v1.7.0** is
the truthful semantic-version target, subject to final exact-SHA validation.

## External final receipt requirements

This tracked document is intentionally a source-bound **pre-release** ledger.
It cannot contain its own final commit SHA or a later redownload result without
moving the tag it is meant to describe. Do not rewrite the tagged source after
publication. The immutable initial provenance plus the distinct signed and
attested post-redownload final-release receipt and its reconciled GitHub release
body must serve as the separate final receipt and record:

1. The exact 40-character final candidate SHA and annotated `v1.7.0` tag
   resolution.
2. Reconcile this ledger against any dedicated P3a/P3c/P3d/P3e receipt that
   lands after this draft, and confirm the final P10 generated artifacts still
   pass their contradiction gate; do not invent evidence IDs when no receipt
   exists.
3. Record the exact final-candidate format/check/Clippy/test results, including
   ignored/external fixture counts, without copying intermediate counts forward.
4. Record newest exact-SHA CI and adversarial workflow run URLs/IDs,
   conclusions, and attempts. A stale run is not acceptable.
5. Record the two clean unsigned Windows build hashes and comparison result,
   final BuildIdentity digest, SBOM and dependency-inventory hashes, exception
   status, vendor-verifier result, and checked technical-review digest.
6. Record the release asset/checksum/provenance hashes, Sigstore bundle and
   certificate identity, GitHub attestation result, and explicit
   `Authenticode: unavailable; PE unsigned` disposition.
7. After publication, record the GitHub release URL and the independent
   redownload verification result: checksum/signature/attestation, package
   notices, SBOM/inventory association, exact tag/SHA/clean BuildIdentity,
   FFmpeg identity, shader digest, and executable `--version-json` result.
8. Leave every unexecuted physical/performance gate above open unless a new
   machine-readable receipt from the named apparatus/workload is actually
   produced. Publication is not permission to rewrite `not executed` as pass.

Until those external records exist and independently verify, this document is
a comprehensive tracked **pre-release** receipt only. It must never be cited
by itself as proof that v1.7.0 was tagged, signed, published, or verified from
a clean download.
