# v1.9.0 perfection pre-release source receipt

Date prepared: 2026-08-27

Status: **pre-release source evidence; not a final release receipt**

Candidate version: **v1.9.0**

Candidate commit: **the exact commit containing this receipt**

Exact-commit release CI, adversarial verification, pre-tag qualification,
annotated tag creation, signing, attestation, publication, and redownload:
**not claimed by this checked-in receipt**

This source receipt binds the completed perfection arc after immutable
published v1.8.1. It does not move, replace, or reinterpret the v1.8.1 tag,
release, assets, or historical evidence. At preparation, GitHub has no
`v1.9.0` tag and no draft or public `v1.9.0` release.

The workflow-generated final-release receipt remains the publication record.
This checked-in source receipt must not be amended after the release tag is
created.

## Completed perfection ledger

The release candidate contains the complete post-v1.8.1 sequence on
`feat/web-control-panel`:

| Tranche | Integration commit | Binding outcome |
| --- | --- | --- |
| P4c planar delivery and integration | `f0a6d45`, `2c4dd7b` | Authored opt-in planar delivery is implemented. The integrated 720p/1080p p99 campaign did not pay the paired default-flip gate, so `legacy_rgba` remains the truthful default. |
| Live recorder audio mux | `f6bf26f`, `7fd0221` | A bounded Program PCM tap is muxed only when live capture is running at record start. Runtime loss terminalizes capture, removes the tap immediately, and preserves device-loss truth even while Program transport is paused. |
| D5 straight-alpha action | `75fc87f` | The existing alpha exporter gained its typed application action and operator path; live-recorder alpha remains separately deferred. |
| D3 portable-show-bundle UI | `49d166a` | Export, preview, and import have an operator surface. Cross-machine promotion remains an explicit second-machine gate, not a hosted-CI claim. |
| Decoder process-exit drain | `e131942` | Process exit uses the settled bounded drain; render-thread `ThreadedDecoder::drop` joining remains rejected by invariant. |
| Performance recorder v2 | `4c0618e` | Append-only address families, engine-owned value laws, real-action replay, and stored-v1-take compatibility are landed. |
| Monitor-bay deferred probes | `0b5584e` | NTSC line state, melt mask, and motion-field probes use the bounded armed reduction path and publish snapshot/panel state only. |
| Group-transform authoring | `d43cffa` | Typed action, revision protection, panel editor, and group gizmo scope landed in the required order. |
| Link/LTC evaluation | `7e79ed7` | Both external-clock families have explicit evidence-backed closeout gates; no unproved license, clock, or hardware claim entered production. |
| Maintenance ladder | `96bcb73` through `bfc1d76` | Actions/Cosign trust moved; the bounded web stack migrated; ntsc-rs compatibility was frozen; GPU/UI, YAML replacement, audio/MIDI, and final utility candidates received truthful promotion or STOP rulings. |

P4c exact-commit hosted CI passed at `2c4dd7b0767d16e31f5b36237985abfd85c3b906`
in run `33068097719`. Audio/MIDI exact-commit hosted CI passed at
`7fd0221a66d7ed8d87994eb91bf833e40e4fad1c` in run `33069954650`:
dependency 34 s, Linux 9m43s, macOS 10m01s, and Windows 16m19s. The final
utility integration and its hosted result are closed in
`maintenance-dialog-tls-devtools-utilities-stop-note.md`; this release does
not reinterpret its physical or migration holds.

## Version and release-policy boundary

Only the owned version seats advance:

- `Cargo.toml` and the root `collide-o-scope` package row in `Cargo.lock`;
- `CFBundleShortVersionString` and `CFBundleVersion` in the macOS plist; and
- the manual-dispatch default in `release-trust.yml`.

Unrelated dependency versions that happen to equal 1.8.1 or 1.9.0 are not
release-version seats and remain unchanged. The expected version-only input
identities are:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `Cargo.toml` | 3,141 | `c64b7f15e515fcda1565915e734a4c6c0197b820ef565220409383703bb498ed` |
| `Cargo.lock` | 151,355 | `5d239b6cb6b8d21bbee35d14abd1a1e6cf32f1e2e09fa2c88ab21db557bd8c00` |
| `packaging/macos/Info.plist` | 2,290 | `bda0767d331458b391c43fc74a1f2e1851add0a2c24fcda3f439421cc3710d38` |

The release-workflow structural digest is
`86ddc170589a43b3a4e7cc71cce36c7883682fd1274b170c21185b886c522881`.
The versioned CycloneDX semantic profile is
`28a7e617720766836d822c04f1de3c81387effc170ee2cd10edb8c96116cc0ab`;
the reviewed SBOM-policy source digest is
`24627d3c98aa1cb78805a03ec03f6049539e39b54fc6e238022ddef284ea8d38`.

The pinned SBOM topology remains 359 dependency components plus six target
components and the root (366 total), 360 dependency rows, 856 edges, 35 root
edges, 357 registry components, one Git component, eight local declarations,
and 13 rewritten references. The release receipt is added to both the source
inventory and every validation/mutation-test seat; the bounded inventory rises
from 12 to 13 source evidence entries.

## Truthful deferred boundary

This release does not claim the operator-gated D3 cross-machine receipt, a
live alpha-capable recorder, physical audio/MIDI interface matrices, physical
Link/LTC proof, or restoration of the absent historical `videos/audit.mp4`.
Those are named reopening gates, not hidden release inputs. No synthetic asset
may be minted to turn an unavailable physical or historical gate green.

## Protected local boundary

The release source excludes and does not stage, hash into release inputs,
copy, rename, or alter the three protected root artifacts:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `.da-vinci-canon-pre-refinement-backup-20260822.zip` | 66,225 | `494b63ad0bd96cfb1c7f20a37ad574075a26dd289c14f9f24e71ffe48ab1eea4` |
| `4K_Nature_Cinematography_recorded_with_Nikon_D5300.webm.1080p.vp9.webm` | 56,984,527 | `ee1cfc47671617f8bdf8031dd19cb00f9359e4bf47bdddc7f1fca9df13d034a0` |
| `Black_swan_(Cygnus_atratus).webm.1080p.vp9.webm` | 60,528,641 | `2b51dda28643af61a163d8b3457fd5885c596c51632576a53a6c4c06722630a4` |

`videos/audit.mp4` remains absent and must not be minted. The ignored local
handover, `CLAUDE.md`, addendum mirrors, and memory are not release inputs.

## Remaining immutable release gates

Before the tag exists, the exact committed source must pass release-policy
self-tests, dependency/license checks, generated-capability verification, the
CI-form six-command gate, exact-head hosted CI, explicitly dispatched
adversarial verification, and read-only pre-tag Release trust qualification.
The remote default branch must still equal the frozen candidate, and both tag
and release must still be absent.

Only then may one annotated `v1.9.0` tag be created and pushed. The tag-triggered
CI, adversarial, and Release trust workflows must all pass. Release trust—not a
manual `gh release create`—owns draft creation, the initial ten assets, frozen
final receipt and signature, redownload, verification, and publication. The
public release must be non-draft, latest, and contain exactly 12 assets.

If any operation fails after tag creation, the tag is immutable: advance the
version. Never move, delete-and-reuse, or overwrite `v1.9.0`.

## Deliberate non-claims

This source receipt is not a signature, attestation, public-release receipt,
cross-machine receipt, physical-device receipt, or substitute audit clip. It
does not promote any evidence-held dependency, flip P4c to the default, change
the external/physical resource ceilings, or claim that a source self-test is a
live two-root reproducible qualification.
