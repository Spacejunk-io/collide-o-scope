# v1.7.2 release-recovery pre-release receipt

Date prepared: 2026-08-24<br>
Status: **recovery implementation evidence; not a final release receipt**<br>
Recovery publication target: **v1.7.2**<br>
Candidate binding: **the Git commit containing this receipt; its exact SHA will
be recorded by the annotated tag and final release receipt**<br>
v1.7.2 tag, signing, attestation, publication, and redownload: **NOT RUN**

## Immutable predecessor stops

The `v1.7.0` and `v1.7.1` annotated tags remain immutable failed publication
attempts. `v1.7.0` tag object
`7687d228096791a6d02d194e3c0df9140f52a4c4` peels to
`8a72e0689fdc3687983e1d887f89d68429cfbc20`. `v1.7.1` tag object
`ad8039387591ffde31bef63e618a004fcc90d10b` peels to
`8b82acd299874b28616dfbb71be31e23f0a41dce`. Neither tag has a GitHub
release, and neither tag may be deleted, moved, or recreated.

The historical `v1.7.0` and `v1.7.1` evidence receipts remain unchanged. This
addendum records the later `v1.7.1` stop and adds only facts learned after the
predecessor receipt boundaries.

## v1.7.1 Release trust stop

Exact-tag CI run `32791188555` and Adversarial verification run `32791188553`
succeeded for `v1.7.1`. Release trust run `32791188615` passed its
exact-commit verification gate, then stopped in `Build twice from clean
independent unequal roots` during build A's pre-build source check, before its
source archive, compilation, or output. The canonical Windows build wrapper
used a sterile Git configuration and rejected `source-a` because its status
was not entirely clean. The status rows were captured but not printed, so the
retained job log cannot identify the exact changed path.

The failed job produced no collide-o-scope executable or release asset,
checksum signature, attestation, workflow artifact, draft, upload, public
release, or redownload. Its publish job was skipped. A local forensic replay
at exact `v1.7.1` commit
`8b82acd299874b28616dfbb71be31e23f0a41dce` did not reproduce the retained
failure. That does not overwrite the authoritative remote stop or establish
which runner-specific operation or checkout interpretation made `source-a`
appear non-clean.

## Checkout-boundary correction

The failed workflow used `source-a` both for mutable dependency-policy tooling
and as claimed clean build input A. The recovery removes that overlap:

1. dependency fetch, vendor verification, advisory checks, license checks, and
   dependency-evidence generation run only in a dedicated `policy-source`
   checkout of the verified candidate commit;
2. independent source A and source B are checked out only after all mutable
   policy tooling has completed;
3. immediately before reproducibility preparation, both build checkouts are
   checked under the same sterile Git configuration as the canonical wrapper;
4. the preflight requires each checkout to resolve to the verified commit and
   to have zero staged, tracked, or untracked status rows; and
5. any refusal identifies the exact checkout and emits a bounded JSON sample
   of status rows instead of discarding the only diagnostic witness.

The canonical build wrapper remains fail-closed. No cleanup, reset, ignored-file
exception, or dirty-source fallback is introduced.

The repository also makes LF checkout ownership explicit for `.gitignore`,
`*.tldr`, and `*.dict`, closing uncovered Git-for-Windows normalization paths.
`fuzz/corpus/**` remains opaque binary data through an explicit `-text` rule.
This addresses a concrete dirty-checkout mechanism without claiming that the
unlogged `v1.7.1` status row is known.

## Pre-tag Release trust qualification

`workflow_dispatch` now qualifies only the exact remote default-branch head,
while `release_tag` remains the planned version identity. Dispatch mode
requires the selected event SHA, checked-out commit, and remote default head to
agree, requires the planned tag to equal the Cargo package version, and refuses
an existing remote tag or GitHub release. It executes the exact workflow gate,
policy checks, fresh-checkout preflight, two unequal-root builds, byte
comparison, SBOM generation, deterministic package/provenance assembly,
unsigned package verification, and pinned cosign installation. A final
read-only check requires the remote default head to remain exact and the tag
and release to remain absent. Signing, attestation, workflow artifact upload,
draft creation, release upload, publication, and redownload are skipped in
dispatch mode and remain restricted to an actual `v*` tag-push event.

The tag-push and dispatch paths are mutually exclusive Windows jobs that share
one YAML-anchored step sequence. Dispatch receives only `contents: read` and
`attestations: read`, with no identity-token or write permission. The tag-push
job alone receives the publication and OIDC authority required by its guarded
signing and release steps.

This permits the Windows-hosted release path to earn evidence before another
immutable version tag is minted. A dispatch result is qualification evidence
only; it cannot substitute for the later tag-push Release trust run or for
independent public-release verification.

## Version and evidence continuity

The recovery version is `1.7.2` in `Cargo.toml`, the root package entry in
`Cargo.lock`, both macOS bundle version fields, and the Release trust dispatch
default. The final-receipt source-evidence inventory requires all three
versioned `v1.7.0`, `v1.7.1`, and `v1.7.2` receipts. The two predecessor
receipts remain byte-unchanged; this `v1.7.2` receipt becomes candidate-bound
when committed. Asset names, release URL, source ref, certificate identity,
and final-receipt names continue to derive from the tag.

The ignored improvement audit remains outside Git with SHA-256
`8aa8b84bd86e91ae4b88a2d0955494b1e88d0cfa66afb971e7181ed36285cd5b`.
The three protected untracked artifacts remain excluded from builds and release
provenance and must retain these SHA-256 identities:

- `.da-vinci-canon-pre-refinement-backup-20260822.zip`:
  `494b63ad0bd96cfb1c7f20a37ad574075a26dd289c14f9f24e71ffe48ab1eea4`
- `4K_Nature_Cinematography_recorded_with_Nikon_D5300.webm.1080p.vp9.webm`:
  `ee1cfc47671617f8bdf8031dd19cb00f9359e4bf47bdddc7f1fca9df13d034a0`
- `Black_swan_(Cygnus_atratus).webm.1080p.vp9.webm`:
  `2b51dda28643af61a163d8b3457fd5885c596c51632576a53a6c4c06722630a4`

## Recovery acceptance and stop conditions

Before a `v1.7.2` tag may be created, one exact candidate must pass:

1. release-policy, waiter, verifier, and finalizer self-tests, including hostile
   rejection of policy/build checkout overlap and dispatch/tag confusion;
2. YAML parsing, actionlint, Python AST, PowerShell AST, format, generated
   capabilities, locked check, full tests, and strict Clippy;
3. dependency, license, vendor, and advisory policy;
4. a fresh Windows checkout with `core.autocrlf=true` whose sterile status is
   empty, whose owned text paths reproduce their index bytes, and whose fuzz
   corpus blobs remain byte-identical;
5. two local clean unequal-path Windows builds with byte-identical executable
   and identity hashes;
6. exact-SHA feature and default-branch CI plus explicitly dispatched
   Adversarial verification;
7. an exact-default-head, read-only-authority pre-tag Release trust dispatch
   that completes both Windows builds, package verification, and pinned cosign
   setup; then proves the default head unchanged and the tag and release still
   absent while every signing, attestation, artifact-upload, publication, and
   redownload step is skipped; and
8. exact ref, audit, protected-file, receipt, and workspace hygiene checks.

Only then may one annotated `v1.7.2` tag be pushed. Its tag-push CI,
Adversarial verification, and Release trust runs must all succeed. Release trust
must publish exactly twelve assets, and a separate fresh verifier must confirm
the immutable tag, exact Sigstore identity, all GitHub attestations, package,
SBOM, dependency evidence, FFmpeg identity, shader digest, embedded version,
final receipt, and a second byte-identical download. Until those public records
exist, this document must not be cited as proof that `v1.7.2` was published or
independently verified.
